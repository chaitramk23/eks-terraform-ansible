plugin: amazon.aws.aws_ec2
regions:
  - ap-south-1
filters:
  "tag:eks:cluster-name": "devops-eks-cluster"
  "instance-state-name": "running"
keyed_groups:
  - key: tags.Name
    prefix: name_
compose:
  ansible_host: private_ip_address        # connect via private IP
  ansible_user: ubuntu                     # SSH user
  ansible_ssh_private_key_file: /var/lib/jenkins/.ssh/id_rsa
  ansible_python_interpreter: /usr/bin/python3
  ansible_connection: ssh                  # use SSH instead of SSM
